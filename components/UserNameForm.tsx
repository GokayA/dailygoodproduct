'use client';
import { FC } from 'react';
import { useForm } from 'react-hook-form';

interface UserNameFormProps {}

const UserNameForm: FC<UserNameFormProps> = ({}) => {
  // const {} = useForm<>()
  return <form onSubmit={() => {}}>UserNameForm</form>;
};

export default UserNameForm;
